<?xml version="1.0" encoding="utf-8" ?>
<mopup:PopupPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mopup="clr-namespace:Mopups.Pages;assembly=Mopups"
    x:Class="TikTime.MauiApp.MVVM.View.Customer.SelectOrderPage" BackgroundColor="Transparent">

    <Grid VerticalOptions="End" Padding="10" BackgroundColor="Transparent">
        <Frame CornerRadius="25"
               BackgroundColor="White"
               Padding="20"
               HasShadow="True"
               HorizontalOptions="Fill"
               VerticalOptions="End">

            <VerticalStackLayout Spacing="15">
                <BoxView HeightRequest="5" WidthRequest="50"
                         Color="LightGray"
                         HorizontalOptions="Center"
                         CornerRadius="5" />

                <Button Text=" جدید ترین" 
                        x:Name="AddCustomerButton"
                        Clicked="LastCustomerButton_OnClicked"
                        HorizontalOptions="Fill"
                        ImageSource="person_icon.png"
                        ContentLayout="Left,10"
                  />
                <Button Text="قدیمی ترین" 
                        HorizontalOptions="Fill"
                        ImageSource="person_icon.png"
                        ContentLayout="Left,10"
                        Clicked="OldButton_OnClicked"
                />
               
            </VerticalStackLayout>
        </Frame>
    </Grid>
</mopup:PopupPage>

